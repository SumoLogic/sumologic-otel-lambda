receivers:
  otlp:
    protocols:
      grpc:
      http:

exporters:
  otlphttp:
    traces_endpoint: $OTLP_HTTP_ENDPOINT_URL

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlphttp]
